/** Tempo de exibição de imagens antes de ir para a próxima mídia (ms). */
const IMAGE_DISPLAY_MS = 8_000;

/** Intervalo para buscar playlist ativa de novo na API (ms). */
const PLAYLIST_POLL_INTERVAL_MS = 10_000;

export { IMAGE_DISPLAY_MS, PLAYLIST_POLL_INTERVAL_MS };
